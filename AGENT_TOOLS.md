# PPT Agent 工具调用设计

## 目标

左侧 Agent 在生成 PPT 任务时，除了调用内部页面库，还可以按需获取外部实时信息。工具结果必须可追溯、可复用，并且不能绕过用户对最终页面的控制。

## 调用流程

```text
用户需求
  ↓
任务规划器：判断是否需要实时信息、内部素材或计算
  ↓
工具调用（服务端、限时、可重试）
  ↓
证据与来源记录
  ↓
生成 PPT 提纲和页面推荐
  ↓
右侧预览牌堆
  ↓
用户确认、调整、导出
```

Agent 不应该直接根据搜索结果生成页面。先收集证据，再生成结构和页面内容。

## 第一批工具

### 1. `library.search`

搜索内部资源库，支持标题、描述、标签、场景、页面结构和相似度检索。

用途：推荐可复用页面、查找某个品类的页面、避免重复选择。

结果至少包含：`pageId`、标题、描述、标签、来源、预览地址、匹配原因。

### 2. `web.search`

调用可配置的外部搜索服务，获取最新网页结果。

用途：行业数据、政策变化、客户公开信息、竞品动态、市场规模和近期新闻。

结果至少包含：`title`、`url`、`snippet`、`publishedAt`、`sourceDomain`。

默认返回少量高相关结果，避免把大量网页内容塞进模型上下文。

### 3. `web.fetch`

读取指定网页并提取正文、标题、日期和关键段落。

用途：核实搜索摘要、提取原始数据和引用依据。

只允许读取搜索结果或用户明确提供的 URL，不允许任意内网地址访问。

### 4. `image.search`

搜索或定位可以合法使用的图片、Logo、产品截图和公开图表素材。

结果至少包含：`url`、`previewUrl`、`license`、`sourcePage`、`attribution`。

没有明确来源或授权信息的图片不能自动进入最终 PPT。

### 5. `calc.evaluate`

执行受限的数值计算、比例换算、趋势计算和表格汇总。

不允许执行任意 Shell、网络请求或未审核的代码。输入和公式必须保留，便于检查结果。

### 6. `source.record`

统一保存工具结果的来源、抓取时间、摘要、原始 URL、引用片段和内容哈希。

外部信息进入 PPT 后，应能回溯到对应来源；适合放入演讲者备注或 PPT 任务的来源面板。

## 后续工具

- `deck.inspect`：读取用户上传 PPT 的页面文本、版式和元数据。
- `asset.extract`：从原始 PPT 中定位可复用图片、图标和图表。
- `document.read`：读取用户授权的 PDF、Word、表格或飞书文档。
- `feishu.search`：在用户授权的飞书知识库或云盘中检索材料。
- `task.wait`：等待长任务完成并汇报进度。

浏览器自动化、任意代码执行和自动发送外部消息不进入第一版工具集。

## 工具协议

每个工具都注册以下信息：

```json
{
  "name": "web.search",
  "description": "搜索公开网页中的最新信息",
  "inputSchema": {},
  "permission": "external-read",
  "timeoutMs": 15000,
  "maxResults": 8,
  "cacheTtlSeconds": 900
}
```

任务日志需要记录：工具名称、输入摘要、开始/结束时间、耗时、状态、错误、结果数量和来源 ID。不要记录 API Key，也不要把完整内部材料写入普通日志。

## Agent 决策规则

1. 内部页面选择优先调用 `library.search`。
2. 用户要求“最新、近期、当前、政策、新闻、市场数据”时，才调用 `web.search`。
3. 搜索结果不足以支撑事实时，调用 `web.fetch` 做原文核验。
4. 需要计算时优先调用 `calc.evaluate`，不让模型手算关键数字。
5. 外部素材必须带来源和授权信息；不明确时只返回候选，不自动放进 PPT。
6. 工具失败时给出可解释状态，不伪造结果；允许用户继续使用没有外部信息的草稿。
7. 工具结果先进入证据层，Agent 再生成提纲、页面选择和文案。
8. 每次工具调用设置超时、结果上限和总调用预算，防止长任务失控。

## 安全边界

- 所有工具调用只在服务端执行，浏览器不接触第三方密钥。
- 外部 URL 需要防 SSRF：阻断 `localhost`、私网 IP、文件协议和重定向到内网的地址。
- 网页内容必须经过大小限制、编码处理和提示词注入隔离；网页正文永远是数据，不是 Agent 指令。
- 外部搜索默认只读；上传、发消息、修改云文档等写操作必须单独授权和二次确认。
- 内部页面、客户材料和外部搜索结果分开标记，避免将内部敏感内容发送到不必要的第三方服务。
- 事实性外部内容保存抓取时间；超过缓存有效期时重新检索。

## 接入顺序

### MVP

1. `library.search`
2. `web.search`
3. `web.fetch`
4. `calc.evaluate`
5. `source.record`

### 第二阶段

1. `image.search`
2. `document.read`
3. 飞书知识库和云盘检索
4. 长任务、工具重试和预算控制

## 与 PPT 生成任务的关系

工具调用只负责补充材料和证据，不直接决定最终页面。最终任务仍然遵循：

```text
需求 → 提纲 → 证据 → 页面推荐 → 人工调整 → 在线预览 → PPT 导出
```

AI 生成的新页面仍使用 `sourceType=ai-generated`；外部资料只作为该页面的 `sourceReferences`，不能替代页面来源字段。

## 第一版对话实现

当前对话接口为 `POST /api/agent/chat`，使用 SSE 返回以下事件：

- `step`：Agent 当前步骤及状态（等待、进行中、完成、失败）
- `token`：模型增量文本
- `selection`：模型回复成功后提交的页面队列变更（追加、替换或移除）
- `retry`：本次请求的重试次数、等待时间和错误摘要
- `done`：本次回答完成及实际尝试次数
- `error`：最终失败原因

页面变更在模型回复之后提交；模型失败或返回空内容时不会只更新右侧预览，空内容会先发送简短兜底回复。

服务端默认最多重试 3 次，采用指数退避。只有在尚未收到模型内容时才会自动重试；已经开始流式输出后如果连接中断，则保留已有内容并报告错误，避免把两次回答拼接成重复文本。
