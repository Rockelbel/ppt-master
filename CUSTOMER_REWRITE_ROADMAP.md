# 客户化改稿整体计划

## 目标

销售上传一份已有客户 PPT，输入目标客户名称和可选的客户资料，系统基于原 PPT 的母版、布局、字体、字号、颜色、图片和可编辑结构，生成一份面向新客户的可审阅过程稿。系统不凭空替换客户事实，不把原客户案例、Logo、数据或承诺伪装成目标客户内容，最终由销售确认后导出正式可编辑 PPTX。

## 核心流程

1. 上传源 PPT，创建异步任务并保留原始文件。
2. 提取页面文本、标题、文本框位置/字号、图片对象、页面角色和源客户候选。
3. 逐页生成变更计划：保留、改写、待确认、删除。
4. 对源客户名称做可验证的文本替换；对 Logo、图片内文字、案例、证言、组织、接口、数据和专属承诺做风险识别。
5. 生成保持母版和编辑能力的 PPTX 副本，并生成在线预览。
6. 销售逐页确认计划，可调整动作、填写目标客户资料、上传目标客户 Logo。
7. 按确认后的计划重新生成副本，保留原页面顺序和版式结构。
8. 执行交付门禁：源客户文字残留、源客户品牌图片、待确认页面、空页/占位页、页面数量顺序、PPTX ZIP 完整性和文本可编辑性。
9. 未通过门禁的文件只能标记为过程稿；通过门禁的文件才标记为可交付稿。
10. 记录任务日志、每页决策、替换记录、残留项、缺失资料和模型调用结果。

## 三种任务状态

- 计划中：完成提取和逐页判断，尚未生成副本。
- 过程稿：已生成可编辑副本，但存在待确认页面、品牌素材或质量门禁问题。
- 可交付稿：销售确认后重跑，门禁全部通过，可导出给客户。

## 变更动作规则

- 保留：通用产品、服务、方法和公司能力，内容不依赖源客户事实。
- 改写：封面、目录、章节定位或仅包含可验证源客户名称的文本。
- 待确认：需要目标客户行业背景、现状、痛点、数据、组织、案例、Logo、接口或专属承诺。
- 删除：明显属于源客户、无法可靠改写且不应出现在目标客户方案中的页面。

## 质量门禁

必须阻断正式交付的情况：

- XML 文本仍出现源客户名称或别名。
- 封面、章节页或客户案例页仍保留源客户 Logo/品牌图片，且没有目标 Logo 或人工确认。
- 客户证言、客户数据、组织架构、接口细节、实施结果被当成目标客户事实。
- 页面为空、标题是占位文本、页面顺序破坏开场/目录/内容/尾页结构。
- 导出文件不是完整可打开的 PPTX，或编辑对象被图片化。

图片处理策略：第一版不做图片内 Logo 的自动擦除或伪造替换。系统识别封面、案例和品牌区域的图片对象并标记待确认；销售可上传目标客户 Logo，后续只在明确的图片框中替换真实素材。

## 前端交互

- 导入记录中展示源客户、目标客户、处理页数、改写页数、待确认页数、品牌素材风险和当前交付状态。
- “变更计划”中每页展示缩略图、标题、动作、原因、源文本摘要和待补资料。
- 支持逐页修改动作，批量确认通用页，填写目标客户资料，上传目标 Logo；计划中可直接查看改写前后页面缩略图。
- 点击“按确认计划重跑”生成新版本，不覆盖源文件和旧过程稿。
- 预览、下载 PPTX 和查看质量报告使用同一版本 ID，避免下载到旧稿。

## AI 使用边界

- DeepSeek 负责意图识别、逐页风险分类和短原因，不直接生成 PPT XML。
- 模型输出必须是严格 JSON；失败时按批次重试，仍失败则降级到规则计划并记录日志。
- 模型不得推断目标客户不存在的事实，不得把第三方案例改成目标客户案例。
- 涉及实时客户资料时，先读取用户提供的文档或经授权的外部工具结果，并保存来源。

## 迭代与 PMF

### MVP 验证

- 用中国移动方案改成可口可乐，验证 100 页级别源 PPT 的异步处理、计划审阅和可编辑输出。
- 销售在 10 分钟内能看懂哪些页被保留、改写、删除或待补资料。
- 一次任务能产生过程稿和质量报告，不修改原 PPT。

### PMF 指标

- 从上传到首份可审阅过程稿的等待时间。
- 销售确认计划的平均耗时和每页人工修改次数。
- 可直接进入客户沟通的页面比例。
- 源客户品牌/事实残留率，目标为正式稿 0。
- 复用页面占比和同一销售二次使用率。

### 后续阶段

1. 目标客户资料包：支持文档、Logo、品牌规范和客户现状资料上传，绑定任务版本。当前已完成私有保存、文件类型/大小校验、TXT/MD/CSV 证据抽取（行号、哈希、版本）和来源记录；PDF/Word/Excel/图片暂标记为未抽取，不自动读取或编造目标客户事实。
2. 图片素材替换：在确认的图片框中替换真实目标 Logo/截图，保留裁切和比例。
3. 内容改写：基于资料包改写客户背景、痛点、案例和下一步，所有事实带来源。
4. 方案结构：按公司已学习的客户整体方案模式生成章节提纲，保证开头、目录、章节转场、案例证据链、服务和尾页完整。
5. 对话协作：一个对话绑定一个 PPT 任务，支持补页、调序、局部替换和历史版本。当前已支持在左侧对话附加源 PPT，并用“改成/改为/面向目标客户”创建异步客户化改写任务；逐页确认、重跑和版本记录仍在任务页完成。
6. 单页/整套生成：沿用现有模板页，不跨来源凑版式；缺少匹配版式时先标记需要新建页面。
7. 多人线上化：任务队列、权限、版本、分享链接、操作日志和大规模资源检索。

## 回归样例

- 中国移动 → 可口可乐：品牌残留、案例待确认、封面和目录改写、可编辑性。
- 蓝箭航天 → 新客户：检测客户证言、案例数据和图片品牌。
- 无明确源客户 → 指定目标客户：要求销售确认源客户和品牌素材后再执行。
- 只改一页：确认不会误生成整套 PPT。

## 当前决策

客户化输出按质量门禁分为过程稿、待确认和可交付稿。逐页确认、删除/改写重跑和目标客户 Logo 图片框替换已经落地；没有目标客户资料、Logo、案例证据或仍有占位页时，系统必须停在过程稿/待确认，不得伪装成可交付稿。

## 2026-08-24 当前落地边界

已验证的最小闭环是“上传、抽取、计划、证据、生成过程稿、在线预览、质量门禁、可编辑 PPTX 下载”。源页计划和最终输出计划分离保存，新增缺口页可追溯到证据 ID，任务重启后可以继续。

当前明确不自动交付的情况：

- 没有客户资料时，四类客户缺口保持 pending，不生成客户事实。
- 源客户长段竞品事实仅做客户名替换时，质量门禁标记 unsupported-customer-rewrite，不允许进入可交付状态。
- 源客户 Logo 未提供目标替换素材时，源品牌图被移除并要求人工确认。
- 占位标题、缺少目录或顺序不符合完整销售方案结构时，任务保持 review。

性能策略：无客户资料时跳过逐页模型分类；有资料时才按 4 页批次调用 DeepSeek，失败自动重试，仍失败回退规则计划并记录 AI 调试日志。106 页真实回归已验证该策略能避免长时间无价值调用。

下一阶段优先级：

1. 在任务页把质量门禁 blocker 直接映射为可执行动作，例如补充资料、上传 Logo、将页面改为删除或待确认。
2. 将 PDF、Word、Excel 资料抽取接入证据流水线，保留行号/页码/版本来源。
3. 基于现有 PPT 与飞书知识生成章节级方案计划，先显示结构再允许页面进入右侧预览。
4. 用真实销售确认时长和最终交付页比例评估 PMF，再扩大整套自动生成范围。
