# PPT Lego Studio 第一版 PRD

## 1. 产品判断

当前最值得验证的不是“AI 能否一次生成最终 PPT”，而是销售能否快速建立一个可复用的页面库，并在 AI 预标注的帮助下完成组合。

产品最小闭环：

> 原始 PPT 收集 → 页面级拆分 → AI 预标注 → 人工确认 → 页面库 → 新方案组合 → HTML 过程稿

PPT 导出先作为输出适配器，是否需要完全可编辑取决于销售拿到结果后的二次编辑需求。

## 2. 目标用户与场景

- **主要用户：销售 / 售前**：上传历史 PPT，修正页面标签，快速拼装新方案。
- **次要用户：方案运营 / 设计**：维护页面库质量，淘汰不适用页面，沉淀品牌规范。
- **暂不服务：普通客户自助生成**：冷启动阶段先解决内部生产效率。

## 3. 用户流程

### A. 收集与拆解

1. 上传 `.pptx` / `.ppt` 文件。
2. 系统创建拆页任务，显示文件名、页数、处理状态和错误信息。
3. 解析服务输出每页缩略图、原始文本、图片素材引用和基础版式信息。

### B. AI 预标注与人工校正

1. 系统将页面内容发送给 DeepSeek v4 Flash。
2. 模型返回页面类型、主题、适用场景、核心结论、关键词和置信度。
3. 人工在页面级工作台修改标签和描述，确认后进入“在库”。
4. 页面连续 3 次被标记为“不适用”时进入“弃用”。

### C. 组合与生成

1. 销售输入方案目标或选择一个提纲。
2. 从页面库筛选并拖拽页面，组成方案顺序。
3. 系统生成 HTML 过程稿，并保留页面来源和版本。
4. 后续再根据编辑需求提供图片型 PPT 或可编辑 PPT 导出。

## 4. 页面标签（MVP）

- 页面类型：封面、目录、问题、洞察、方案、产品能力、案例、数据、流程、报价、行动召唤、结尾
- 适用场景：首次拜访、方案汇报、产品介绍、行业案例、异议处理、复盘
- 主题关键词：3-8 个
- 核心结论：一句话
- 内容状态：在库 / 弃用
- AI 置信度与人工确认状态

不在 MVP 中维护独立“风格”字段，统一继承公司品牌规范。

## 5. MVP 验收标准

- 上传后 10 秒内能看到任务进入队列，失败有可读原因。
- 一份 10-15 页 PPT 可以逐页查看，页面不丢失顺序。
- AI 预标注至少覆盖 80% 页面，人工可以在同一界面修正。
- 从页面库手动拼装一份新方案在 1 小时内完成。
- 可以在线预览 HTML 过程稿，并看到每页来源。
- 所有修改有版本记录，页面支持弃用但不物理删除。

## 6. 明确不做

- 元素级 PPT 编辑器（使用飞书文档或原生 PPT 编辑能力）。
- 直接从录音生成最终交付稿。
- 一开始就训练私有模型或构建复杂向量平台。
- HTML / PPT 双源人工同步。
- 多租户、权限矩阵、外部客户协作。
- 自动删除重复页面。重复检测先作为候选提示，避免损失有不同客户语境的页面。

## 7. 客户化改稿扩展

销售可以上传一份已有客户方案，例如把中国移动版本改为面向可口可乐的版本。该流程与普通资源库导入分开，系统保留源 PPT，不做图片化或全文盲替换。

- 先提取源 PPT 的页面、文字、标题和图片，生成逐页保留、改写、待确认、删除计划。
- 销售确认计划后才生成副本；目标 Logo 和客户资料包作为任务来源绑定。
- 只把可验证的客户名称和文案替换到明确页面；客户案例、数据、组织、接口和专属承诺没有证据时必须停在待确认。
- 计划中显示改写前/改写后缩略图，输出同时提供在线预览和可编辑 PPTX。
- 质量门禁检查源客户文字、品牌图片、页面数量、PPTX 完整性、可编辑文本和占位页；不通过时只能作为过程稿或待确认稿。
