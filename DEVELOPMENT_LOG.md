# PPT Master 开发记录

更新时间：2026-08-23

## 当前已完成

- 已导入 25 份 PPT，共 444 页真实页面资产。
- 已生成逐页高清预览图、页面 XML 文本、文本框位置和字号元数据。
- 资源库支持标题、结构标签、场景标签和一句话描述展示。
- 页面可加入右侧预览牌堆，支持收起、展开、拖拽排序、删除、清空和在线预览。
- 支持按当前页面顺序导出原始 PPT 页面。
- 详情抽屉支持标签、关键词、描述和标题编辑。
- 详情抽屉显示页面唯一 ID，便于问题定位。
- 已过滤纯数字标题、页码标题和页码描述噪声。
- DeepSeek 配置已放在服务端 `.env`，浏览器不直接接触 API Key。

## 当前数据状态

- 标题识别：XML 位置/字号规则，非大模型。
- 一句话描述：已由 DeepSeek 根据页面标题和 XML 全文生成，长度按页面信息量自适应。
- AI 标注：444 页均已完成并批量确认；0 失败。
- “待补充标题”：本轮已逐页复核并补齐 11 页图片型页面标题，目前为 0 页。
- 去重：尚未自动去重，只保留页面来源和文本元数据。

### DeepSeek 页面预标注试点（2026-08-23）

- 已接入服务端 DeepSeek 批量标注任务：逐页任务、状态持久化、失败重试、结果写回页面索引和源文件元数据。
- 曾增加资源库 AI 状态展示和批量预标注入口；现已由导入任务后台统一执行，AI 结果仍进入“待确认”，不会覆盖人工确认结果。
- 已完成首批 30 页试点：30/30 成功、0 失败。
- 已完成剩余 414 页批量标注：414/414 成功、0 失败；服务端使用 3 路模型请求并行，数据写入串行合并，避免页面结果互相覆盖。
- 已将 444 页 AI 结果批量标记为已确认，后续可直接用于筛选和组合。
- 已增加质量检查报告：当前发现 155 个重复候选（125 个文本完全相同、30 个标题相同且正文高度相似）和 1 个缺少预览图的问题；候选只提示，不自动删除。
- 已将 188 个唯一重复/质量候选页标记为 `libraryStatus=excluded` 并从资源库隐藏，原始数据和质量报告保留；当前资源库展示 256 页。
- 输出字段：内容描述、结构标签、场景标签、最多 5 个关键词。
- 资源库筛选区新增“状态”筛选，可快速查看待标注、标注中、待确认和已确认页面，并显示各状态数量。

### 导入历史与异步导入工作流（2026-08-23）

- 导入入口支持多选 PPT/PPTX，服务端为每个文件创建独立任务并持久化到 `.tmp/import-tasks.json`。
- 导入记录页面展示文件、阶段、总页数、已提取、AI 标注、重复候选和最终状态，支持刷新和轮询。
- 上传后后台按 `拆分页面 → XML 文本/布局提取 → 规则预标注或 DeepSeek 标注 → 精确文本重复匹配 → 入库` 执行。
- 每页原始文本、标题来源、文本框位置/字号、标签结果和重复关系会写入资源元数据；页面预览图由原始 PPT 渲染生成。
- 重复页不会物理删除，导入结果会标记为 `libraryStatus=excluded`，并在导入记录中保留 `duplicateOf`。
- 新导入页面会保留原始 PPT 文件，后续仍可按页面顺序导出可编辑 PPTX。
- 资源库中的“质量检查”和“AI 批量预标注”按钮已移除；这两步统一由导入任务后台执行，避免重复入口和重复调用。
- API Key 只在服务端 `.env` 读取，浏览器不会获取或保存密钥。
- 服务启动时会自动恢复未完成的导入任务，避免服务重启后任务停在处理中。

### 公司 PPT 知识提炼（2026-08-23）

- 已基于当前 26 份来源 PPT、550 页页面元数据和 414 份布局检查文件生成 `data/ppt-patterns.json`。
- 已生成可读分析文档 `PPT_KNOWLEDGE.md`，覆盖整套方案组织模式、页面结构模式和设计风格证据。
- 组织分析包含 PPT 类型、前三页开场组合、页面/场景转场和每份 PPT 的完整页面顺序。
- 页面分析包含结构标签、场景分布、页面密度、标题识别来源和可复用的版式特征签名。
- 设计分析包含版式尺寸、布局名称、字体、字号和颜色统计。
- 第一版 Agent 约束已写入分析文档：先识别任务类型和提纲，再选择页面，最后进入预览牌堆由人工确认。
- 当前分析是可解释的统计基线，不等同于已经训练完成的生成模型；章节关系、案例论证关系和视觉型页面仍需继续补充。
- 已统一页面素材来源字段：现有 550 页标记为 `sourceType=imported`；未来 Agent 生成页面只有在用户明确加入素材库后才标记为 `sourceType=ai-generated`，并保留任务、模型和模板来源。
- 已整理 Agent 外部工具调用设计，见 `AGENT_TOOLS.md`：第一批包含内部资源检索、网页搜索、网页读取、受限计算和来源记录，并定义了工具预算、SSRF 防护、来源追溯和人工确认边界。
- 已完成第一版 Agent 对话基础链路：`POST /api/agent/chat` 使用 SSE 流式输出，前端只展示精简纯文本；页面队列变更在模型回复成功后提交，空响应有兜底回复。
- Agent 请求在未产生模型内容前支持最多 3 次指数退避重试；流式输出中断后不会重复拼接内容，会保留已收到内容并显示错误。
- 对话已增加意图分流：能力咨询、知识问答和 PPT 规范咨询走普通回答；明确的生成、制作、选页、修改、补页和导出请求才进入 PPT 工作流并展示步骤。
- Agent 文本输出统一清理 Markdown 标记、项目符号、编号、表格和代码块，前端只展示纯文本。
- 已将方案推荐从单纯关键词拼页升级为质量门槛：先识别主题和方案类型，过滤内部提醒/内部材料与不相关竞品页，再按主题集中度选择锚点 PPT；同一锚点来源不足时宁可少选，不跨来源凑页。
- 封面、目录、尾页单独校验；缺少同主题可复用页面时明确返回“需要新建页面”，不再拿无关页面作为结构占位。
- 页面变更结果仍由右侧预览承接，推荐结果仅作为候选草稿，最终需人工确认。

## 待办事项

### 客户化改稿质量门禁增强（2026-08-23）

- 扩展 `tools/check_customer_rewrite.mjs`：现在会核验封面、目录、尾页是否存在且顺序合理。
- 变更计划页码必须完整覆盖源 PPT，动作必须有效；计划预计页数必须与实际 PPTX 页数一致。
- 输出质量报告增加 `planMapping`、`roleCompleteness`、`issueCounts` 和可交付状态，支持源 PPT 对照检查。
- 支持传入 `--source` 对源页面和输出页面进行映射；标记为 `retain` 的页面文字发生变化会阻断交付。
- 输出页面会再次扫描源客户名称和替换前文案，避免只依赖替换报告；保留旧客户残留、旧文案残留和占位标题检查。
- 对客户事实、`facts`/`claims`/`customerFacts`/`verifiedClaims` 以及 `evidenceIds` 做 `evidenceId` 存在性校验，防止无法追溯的客户事实进入交付稿。
- 增加标题与描述重复、相邻页标题重复检查；这些问题进入待复核，不会静默放行。
- 新增 `tools/test_check_customer_rewrite.mjs`，覆盖完整映射、页数不一致、非完整结构、源客户残留和证据追溯阻断。
- 客户化任务调用门禁时传入源 PPT 路径，质量门禁结果同步写入任务 `qualityGate`。

### 任务规格解析基线（2026-08-23）

- 新增 `tools/task_spec.mjs`，把用户请求解析为可记录的任务规格：新建单页/多页、修改、客户化改稿、删除、调序、直接复用、追加和导出。
- 页码引用与页数请求分离：`修改预览第二页`、`第 2 页` 不会再被识别为生成 2 页；只有未带“第”的明确数量表达才会产生 `requestedPageCount`。
- 服务端 `/api/agent/chat` 现在先记录并发送 `task-spec` 事件，再用任务规格约束模型意图路由；修改类请求暂时进入原推荐分支，避免继续误入多页生成流程。
- “把中国移动方案改成面向可口可乐”被识别为 `modify_deck`，记录输入来源为上传/现有方案并提取目标客户，后续接入整套 PPT 局部替换执行器。
- 当前限制：任务规格已记录，但服务端尚未持久化预览队列版本，也未实现按目标页 ID 的局部替换；这属于下一阶段执行器工作。

### 预览队列权威状态（2026-08-23）

- 新增 `tools/preview_state.mjs`，以会话 ID 保存有序页面 ID、单调递增版本和最近操作日志。
- 服务端新增 `/api/preview/sessions/:sessionId` 查询/初始化接口，以及 `/operations` 原子操作接口，支持 append、replace_at、remove_at、reorder、replace_all。
- 所有操作支持 `expectedVersion` 乐观并发校验，目标页操作支持 `expectedPageId` 校验；版本冲突返回 409 和当前队列快照，避免长任务覆盖用户最新调整。
- `/api/agent/chat` 在开始时发送 `preview-state`，selection 先提交服务端队列，再把服务端确认的 pageIds/version 推给前端；生成、推荐、完整方案分支均接入该协议。
- 前端人工添加、移除、清空、拖拽调序、弃用页面均同步原子操作，保留 localStorage 作为兼容缓存。
- 回归：`node tools/test_preview_state.mjs` 覆盖五类操作、页面位置校验和版本冲突；`node --check server.mjs && node --check app.js` 通过；HTTP 4187 端口验证创建、追加、读取和持久化成功。

1. 人工处理质量检查中的重复候选，决定保留、弃用或合并来源。
2. 对无文本页面增加 OCR/视觉识别，并提供人工确认入口。
3. 为导入 worker 增加失败任务重试、取消任务、导入详情页和批量任务聚合视图。
4. 让左侧 AI 对话真正支持模型调用、当前预览上下文、历史任务和长任务状态。
5. 增加页面相似度检测，先提示重复候选，再由人工确认保留或弃用。
6. 增加服务端持久化、账户权限和多人协作支持。
7. 页面数量扩大后增加分页/虚拟列表、缩略图懒加载和筛选索引。
8. 支持从零开始通过对话生成一页新的 PPT，并沿用公司模板和页面规范。
9. 资源库页面增加“添加到对话”，允许对选中的页面提出修改、补页和重组要求。
10. 支持通过对话从零生成一整套 PPT，输出可审核的页面顺序和预览牌堆。
11. 分析现有 PPT 的组织模式，包括页面顺序、章节关系和不同销售场景的组合规律。
12. 分析页面结构和设计风格，包括版式、字体、字号、颜色、间距、图片和组件使用方式。

## 后续工作方向

### 第一阶段：AI 标注闭环

以已有 444 页元数据为输入，批量调用 DeepSeek 生成可审核的页面级摘要和标签；模型结果写入 `source-metadata.json` 的 `aiLabeling`，人工确认后的结果再写入 `pages.json`。

### 第二阶段：对话驱动的页面生产

对话任务分为三种模式：从零生成单页、引用资源库页面进行修改/补页、从零生成整套 PPT。每个任务绑定独立的预览牌堆，模型先返回页面选择和结构计划，再由用户确认后生成页面内容。

### 第三阶段：公司内部 PPT 知识

基于页面元数据和人工确认结果，建立内部页面组织模式、页面结构模式和设计风格知识。该知识作为 Agent 生成单页和整套方案的约束，不直接套用通用 PPT 范式。

### 第四阶段：素材资产质量

补齐图片型页面的 OCR/视觉标题，建立标题、正文、标签和描述的去重及质量检查，避免页码、文件名和坐标信息进入用户可见字段。

### 第五阶段：AI 工作台

围绕一个预览任务建立对话上下文，让 Agent 能基于公司内部页面顺序和版式知识补页、调序和生成整套 PPT；同时保留人工拖拽和逐页确认作为最终控制。

### 第六阶段：多人线上化

再引入服务端任务队列、权限、历史版本、分享链接、并发处理和大规模页面检索，满足局域网及后续公网访问。

## 单页 AI PPT 迭代（2026-08-23）

### 目标

- 用户在左侧对话中提出“新建一页”请求。
- DeepSeek 只负责生成结构化页面文案，不直接生成 PPT XML。
- 服务端基于现有公司 PPT 选择模板页，复制原始页面并替换继承的文本框。
- 输出正常可编辑 PPTX 和高清预览图，先作为右侧预览草稿，不自动进入资源库。

### 已完成

- 新增 `tools/generate_page_ppt.mjs`，使用 `@oai/artifact-tool` 导入 PPTX、复制源页、编辑原始标题和正文文本框并导出 PPTX。
- 已验证模板页 `deck-01-p005`：标题、正文替换后仍保留原始图片、分割线、字体、字号、颜色、版式和可编辑性。
- 服务端新增单页生成分支：识别“新建一页/做一页新的/生成一页”等意图。
- DeepSeek 单页内容使用 JSON 协议：`title`、`subtitle`、`body`、`pageRole`，并做 JSON 解析、字段校验和重试。
- 当前模板选择规则：酒店使用 `deck-01-p005`，AI/Agent 使用 `deck-03-p003`，竞品使用 `deck-10-p005`，其他请求使用酒店正文模板作为默认候选。
- 生成页保留 `sourceType`、`sourceTaskId`、`sourceModel`、`sourceTemplatePageId`、`generatedAt`、`generatedPptx` 等来源元数据。
- 生成页写入 `.tmp/ai-generated-pages.json`，不写入正式资源库；服务重启后仍可恢复草稿页面。
- SSE 新增 `generated` 事件，前端收到后将生成页加入页面内存和右侧预览队列。
- 详情抽屉对 AI 草稿显示“加入资源库”，确认后才写入 `data/pages.json` 和 `data/pages.js`。
- AI 草稿支持单页 PPT 下载；当前不允许与存量页面混合导出，避免不同导出来源的语义不清。
- 已通过接口验证：DeepSeek 返回 JSON、PPTX 生成成功、文件类型为 Microsoft PowerPoint 2007+、下载接口返回 200。

### 当前限制与后续处理

- 当前正文替换采用模板页文本框的长度和位置启发式，后续需要为更多模板页建立明确的文本框角色映射，避免复杂页面替换到不合适的文本框。
- 当前生成页只完成内容页，封面/目录/尾页以及整套页面生成仍未开放。
- 当前生成页暂不支持与存量页面混合导出；下一步需要统一导出器的数据源，让 AI 页面和导入页面按顺序合并导出。
- 当前生成内容没有外部搜索工具；涉及实时信息的请求应先走搜索和来源确认，再生成页面。
- 浏览器自动化验收环境当前不可用，已完成服务端接口、PPTX 文件和静态语法检查；前端需要在本地浏览器手动验证一次。

## 完整客户方案基准复盘（2026-08-23）

### 对照对象

`中国移动-分贝通企业支出管理解决方案V4.pptx` 共 106 页。它不是普通项目/内部汇报，而是一份完整的客户整体方案，包含公司可信度、客户案例、整体产品、商旅模块、降本管控、合规、AI/Agent、服务与 SLA。

### 新学习

- 完整方案不能用 10 页关键词推荐替代，当前基准可见页面 83 页，足以形成 80 页以上的长方案初稿。
- 长方案会重复使用目录页作为章节边界；目录页两侧的宏观章节转场不能因为跳过目录而丢失。
- “降本与管控”需要拆成低价降本、数据/管控降本、特色方案、合规筛查；“AI 与 Agent”需要拆成商旅 Agent、管控 Agent、AI 砍价与 Skills；服务需要拆成客服和 SLA/增值服务。
- 客户案例应形成客户背景、合作模式、实施/流程、结果/证言证据链，而不是单独插入一张案例页。
- 只有整套方案第一页应标记为封面，章节标题页应从封面中分离为章节页；标题型章节页不能混入普通内容页。
- 第 106 页是空占位，不是尾页。完整客户方案必须补充总结、下一步和联系方式页。

### 规则修正

- `tools/analyze_ppt_patterns.py` 新增“客户整体方案”和“客户专项方案”分类。
- 分类优先级改为先识别客户方案，再识别内部汇报；“产研能力”不再触发内部汇报分类，只有“启动会、操作手册、阶段工作、项目复盘、内部汇报、周报”等完整语义才算内部信号。
- `data/ppt-patterns.json` 已将 deck-26 修正为“客户整体方案”。
- `PPT_KNOWLEDGE.md` 已增加客户整体方案默认结构和尾页质量门禁。
- 整体方案请求已接入 Agent：以 deck-26 的可见页面为基准，生成 80 页以上的完整章节预览，并明确标记需要新建尾页。
- 页面导出器已支持导入页与 AI 生成页混合合并导出；AI 草稿仍需用户确认后才进入资源库。

### 验收结果

- “请生成一套分贝通产品整体介绍方案，面向中国移动做大型客户方案汇报”第一轮生成 82 页长方案草稿，第二轮补齐正式尾页后形成 83 页可编辑方案。
- 方案顺序覆盖：封面/目录、公司与平台、客户案例、整体产品、商旅、降本管控合规、AI/Agent、服务与 SLA。
- 质量门禁正确发现：缺少正式尾页，需要继续生成总结/下一步/联系方式页；不再把第 106 页占位页作为尾页。

## 完整方案第二轮验收与规则迭代（2026-08-23）

### 生成结果

- 以 `deck-26` 的已确认且未隐藏页面为主体，追加一张沿用同一客户方案版式的 AI 生成尾页，生成 83 页可编辑 PPTX：`output/benchmarks/china-mobile-83-page-plan.pptx`。
- 尾页使用中国移动方案自身的版式和资源，不再跨来源拼接黑底结束页；渲染后确认不再出现空白页或丢失图片资源。
- 逐页渲染 83 页并完成代表性开场、章节边界、案例、服务和尾页检查；PPTX 文件类型、压缩包完整性和文本可编辑性检查通过。

### 页面角色与机器规则

- 为 550 页页面写入 `pageRole`、`roleSource`；中国移动方案额外写入 `chapterId` 和 `chapterTitle`。
- 页面角色包括封面、目录、章节页、客户背景、案例、公司可信度、整体方案、模块总览、模块详情、价值证据、管控合规、AI/Agent、服务、尾页和占位页。
- 机器可读角色统计写入 `data/page-role-summary.json`，并同步到 `data/ppt-patterns.json`，供 Agent 先规划角色再选页。

### 质量门禁迭代

- `tools/evaluate_sales_deck.mjs` 现在评估实际 83 页方案，而不是把原始第 106 页占位页误当作尾页。
- 门禁新增占位页、缺标题、连续重复标题检查；当前结果为封面、目录、章节页、尾页齐全，无占位页，仅提示第 90 页与第 89 页标题同为“AI审批”，需人工确认是否为不同流程的连续页面。
- 结论：重复标题不是自动删除条件，应结合正文和视觉差异判断；相同标题但内容不同的连续页保留，并在导出前提示销售复核。

### Agent 长任务迭代

- 新增服务端 `.tmp/agent-tasks.json` 持久化，`POST /api/agent/chat` 返回任务 ID，记录任务模式、状态、选中页面、生成页面、重试次数和错误摘要。
- 新增 `GET /api/agent/tasks`，服务重启后可读取最近任务；浏览器本地对话历史仍保留，后续可将历史会话迁移到服务端。

## 单页 JSON 响应修复（2026-08-23）

- 复现了“模型没有返回 JSON”：DeepSeek 请求成功，但单页链路原先只处理字符串内容，无法诊断异常响应，失败后直接结束任务。
- `parseJsonModelOutput` 现在兼容字符串、内容数组、JSON 字符串二次包裹、Markdown 代码块及 JSON 前后说明文本，并校验最终必须是对象且包含页面字段。
- 单页请求第 1 次保留 JSON response format；重试请求增加严格修复提示并移除 response format，避免同一失败条件重复请求。
- 新增 `.tmp/ai-generation-debug.jsonl`，只记录状态、finish reason、内容类型和截断摘要，不记录 API Key。
- 多次模型解析失败时使用明确标记为 `fallback` 的最小页面内容继续完成模板生成，避免任务卡死；生成页元数据会保留失败原因。
- 增加事实约束：单页模型只能使用用户要求或模板原文中明确出现的数字、能力和权益，避免生成未验证的对客数据。
- 回归验证“新建一页，介绍分贝通酒店资源优势，突出供应商覆盖、直采能力和会员权益”：SSE 依次返回 `generated`、`selection`、`done`，任务为 `completed`；输出为可编辑 PPTX，预览图生成成功。

## Agent 意图路由与流程日志（2026-08-23）

- 复盘发现“添加一企业介绍商旅Agent的PPT”被旧关键词路由为普通聊天，未进入单页生成；问题不在模型 JSON，而在意图分类方案过于脆弱。
- 意图路由改为 DeepSeek 结构化分类，输出 `chat`、`recommendation`、`single_page`、`full_deck` 四类 action；仅在模型不可用或连续失败时使用本地规则兜底，并记录 fallback 原因。
- 单页流程新增 `.tmp/agent-flow.jsonl` 节点日志：收到请求、意图分类、上下文加载、模板选择、模型内容、PPTX 导出、页面生成和完成/失败。
- 回归验证原始消息“添加一企业介绍商旅Agent的PPT”：模型分类为 `workflow/single_page`，选择 `deck-03-p003`，首轮模型响应因 `finish_reason=length` 为空，第二轮重试成功，PPTX 和预览图均生成，任务 completed。

## 方案结构根因复盘（2026-08-23）

- 发现当前“相关页面推荐”仍然可能前后无逻辑：推荐函数直接从标签和标题打分，最多选 10 页，没有先生成页面槽位、章节依赖和缺口计划。
- 发现多目标请求会被压缩成单一动作：例如“先做一页产品总览，后面每个产品单独介绍”被识别为 `single_page`，后半段的页面系列要求丢失。
- 结论：继续堆叠关键词或单页提示词不能根治。下一阶段必须增加任务理解、公司知识、方案规划、页面生成和质量校验五层链路。
- 飞书文档适合作为产品事实和销售口径来源，现有 PPT 适合作为页面组织、版式和案例证据来源；两者需要保留文档版本、章节和来源引用，不能直接把全文拼进提示词。
- 已将 Phase 1.6 计划写入 `DEVELOPMENT_PLAN.md`，包括飞书文档接入、任务规格、页面槽位计划、事实追溯和结构质量门禁。

## 飞书知识接入与实时兜底（2026-08-23）

- 使用 lark-cli 用户身份授权成功，读取权限可用；不会把飞书访问令牌写入项目。
- 新增 `tools/fetch_feishu_knowledge.mjs`：按产品总览、商旅费控、AI/Agent、服务 SLA、客户案例、销售方案和降本管控等主题搜索，去重后读取 37 份高相关文档。
- 生成 `data/feishu-documents.json`、`data/company-knowledge.json` 和 `data/source-registry.json`，保留文档 URL、token、版本、更新时间、分类和正文证据；原始文档快照保存在 `.tmp/feishu-knowledge/`。
- 服务端工作流收到任务后会实时调用 lark-cli 搜索最新飞书资料，并串行读取最多 3 篇命中文档；实时检索失败时继续使用本地知识库，不阻塞任务。
- 实时命中的文档标题、版本、更新时间和抓取数量写入 `.tmp/agent-flow.jsonl`，不把完整原文写入日志。
- 回归验证“添加一页PPT，介绍分贝通AI产品总览”：实时检索命中 10 篇、读取 3 篇，随后完成模板选择、模型生成、PPTX 导出和任务完成。
# 2026-08-23 · 工作流关键步骤流式展示

完成 Agent 工作流进度第一版：服务端通过 SSE stage 事件发送请求类型、上下文、飞书检索、模板选择、模型生成、PPTX 导出、预览更新等可验证步骤；前端仅对 workflow 消息渲染紧凑进度区，普通聊天不显示。进度默认显示最近步骤，支持展开查看全部，任务完成后自动收起；重试、失败和检索降级会保留明确状态与结果，不暴露模型内部思考、提示词或 API 细节。

验证：普通聊天请求不渲染工作流步骤；“添加一页PPT，介绍分贝通AI产品总览。”成功收到完整 stage 顺序，并生成可编辑 PPTX、加入右侧预览。
# 2026-08-23 · 指定页数的多页 PPT 规划与资源库决策

修复“两页 PPT 被误当成完整方案并选中 83 页”的问题。新增 multi_page 工作流：解析用户明确页数（阿拉伯数字和中文数字），页数优先于泛化的方案意图；先生成严格等于目标页数的逐页提纲（主题、标题、页面目的、要点），再逐页匹配资源库，输出直接复用、基于相似页调整或新建的决策；最后按顺序更新预览并做实际页数硬校验。

验证请求：做两页 PPT，用来介绍商旅Agent和管控Agent的综合能力、核心功能。结果为 2 页，提纲 2 页；商旅 Agent 命中 deck-07-p011，管控 Agent 命中 deck-15-p008，均判定为资源库直接复用，右侧预览按顺序更新。前端新增“页面提纲与处理决策”区域，展示每页标题、用途、处理方式和命中页面 ID。

# 2026-08-23 · 单页整合页旧模板文案残留修复

问题：单页生成虽然已经完成概要、资源库匹配、模板校验和 PPTX 导出，但通用生成器只替换少量最长正文框。使用 `deck-03-p003` 时，模板中的“为什么需要、服务对象、核心场景”以及个人商旅旧文案会残留，导致商旅+管控整合页语义混杂。

修复：为商旅+管控、一体化、整合请求增加 `travel-control-integration` 页面布局配置；对 `deck-03-p003` 的标题、副标题、三栏栏目、五个流程场景、底部价值区及旧正文逐一替换或清空。导出前增加旧长文案残留门禁，发现未替换内容直接让候选模板失败，不把半成品加入预览。

回归：请求“做一页全新的页面，整合商旅加管控两个产品去进行介绍。”先输出单页概要和“基于相似页面调整生成”决策，最终使用 `deck-03-p003`，生成 1 页可编辑 PPTX 和 PNG。渲染检查确认页面只保留商旅服务、管控能力、一体化流程和客户价值内容；原模板 ToC/个人出行文案不再出现。PPTX XML 中标题和正文仍为独立文本框，文件可编辑。`slides_test.py` 因当前环境缺少 numpy 未能运行，已用 PPTX XML、导出文件完整性和渲染图完成替代检查。

# 2026-08-23 · 产品目标复盘与路线重划

## 复盘结论

项目最终目标确认是：以公司历史 PPT、页面元数据和飞书产品资料为知识底座，支持销售通过对话或录音生成一份有完整叙事、可在线调整、可导出正常可编辑 PPTX 的客户方案初稿；AI 负责理解任务、规划页面、检索和补缺，销售保留页面顺序、内容和品牌的最终控制权。

当前页面资产库、导入拆页、AI 标签、飞书知识同步、流式对话和 PPTX 导出属于正确底座，没有偏离最终方向。偏差集中在“对话驱动产出”层：任务规格缺失、预览队列没有服务端权威状态、修改和新建语义混淆、资源库高分直接复用、模板没有统一槽位契约、导出成功没有等价的业务语义验收。

## 新的执行优先级

后续按照 P0-P5 执行：先冻结范围并建立真实回归集；再实现 task-spec 和服务端预览状态；然后把计划和变更执行分开；随后建立高频页面的模板槽位契约；最后接入事实、语义、视觉质量门禁并用真实销售任务判断 PMF。外部搜索、复杂多 Agent 和超长方案自动生成暂缓，避免放大当前状态问题。

# 2026-08-23 · 客户化改写任务前端入口

- 将“上传一份现有客户 PPT，改写为另一客户方案”纳入核心场景，在导入记录页增加独立的“客户化改写”入口，不改变普通 PPT 导入和入库流程。
- 创建任务时收集源 PPT、原客户（可选）和目标客户；任务列表展示原客户、目标客户、处理页数、替换页数、待确认页数、状态、预览链接和导出链接。
- 前端约定接口：`POST /api/customer-rewrite`，使用 multipart 字段 `file`、`sourceCustomer`、`targetCustomer`；返回 `{ task }`，任务可包含 `id`、`status`、`metrics.processedPages`、`metrics.replacedPages`、`metrics.pendingPages`、`previewUrl`、`exportUrl` 和 `message`。
- 当前服务端尚未实现该接口。收到 404 或服务不可用时，前端将任务保存在浏览器本地并明确标记“待接入”，不伪造页数或导出结果，也不阻塞现有导入工作流。

## 需要产品侧确认的默认规则

建议默认采用：明确“做/生成一页”时新页追加到当前预览；明确“修改预览第 N 页”时只替换第 N 页；明确“直接复用”时才允许原页不改直接加入；出现“整合、重组、只保留、不要其他内容、改标题”时必须调整生成。所有替换和删除操作都要保留非目标页，并在执行后展示前后差异。

# 2026-08-23 · 客户化方案改写纳入主路线

新增核心场景：销售上传已有客户方案，例如中国移动版本，要求改写为可口可乐版本。该能力定义为源方案的客户化改写，不是全文替换客户名，也不是从资源库重新拼一份无关页面。

规则：系统必须逐页输出保留、改写、删除、新建和待补充决策；通用公司/产品/服务页可保留，源客户名称、客户案例、客户数据、组织/接口和专属承诺必须隔离处理。无法从销售输入、飞书资料或可信外部来源验证的新客户事实不得编造，而要作为显式缺口交给销售补充。最终输出保留源 PPT 的真实母版、版式和可编辑元素。

执行顺序：先完成 task-spec、服务端预览队列和局部变更硬校验；随后实现客户化 change-plan、源 PPT 副本改写、事实隔离和导出质量门禁；最后用中国移动 → 可口可乐作为端到端回归基准。

# 2026-08-23 · 客户化改写执行器与端到端回归

完成客户化改写第一版执行闭环：

- 新增服务端异步任务持久化：`.tmp/customer-rewrite-tasks.json`；上传源 PPT 后任务独立运行，支持服务重启后继续处理。
- 新增 `POST /api/customer-rewrite`、`GET /api/customer-rewrite`、`GET /api/customer-rewrite/:id`；任务阶段包含提取、计划、改写、渲染和完成/失败。
- 新增 `tools/customize_ppt.mjs` 逐页 change-plan 输出。改写只发生在上传文件副本上，使用 artifact-tool 导入/导出，保留原 PPT 的母版、版式、图片和可编辑文本结构。
- 原客户字段可选；未填写时从文件名和首批页面文本识别“中国移动”等已知客户。未知来源不会猜测客户名。
- 页面计划支持 retain、rewrite、pending；AI 有效返回时按批次语义判断，单批 JSON 失败时仅该批次退回规则计划，不让整项任务伪装成功或中断。
- `pending` 页面仅展示缺口，不把目标客户的行业、现状、案例、数据、Logo 或专属承诺编造进 PPT。结果报告保留源客户残留、失败替换、待补充资料和逐页原文摘要。
- 任务完成后生成在线 HTML 预览和正常可编辑 PPTX 下载链接；输出不是截图打包。
- 前端导入记录页新增任务轮询、处理页数/替换页数/待确认页数、变更计划 JSON、在线预览和 PPTX 下载入口。

中国移动 → 可口可乐回归：

- 源文件 106 页，输出 106 页；PPTX 压缩包完整，XML 中“中国移动”残留为 0，封面替换为“可口可乐”。
- 输出文件：`output/customer-rewrites/rewrite-1787469618831-4a0c9977/可口可乐-中国移动-分贝通企业支出管理解决方案V4.pptx`。
- 首次规则计划识别 14 页待补充、85 页保留、7 页改写；部分 AI 批次返回非 JSON 时自动降级并写入 `.tmp/ai-generation-debug.jsonl`，没有伪造 AI 成功。
- `node --check server.mjs app.js tools/customize_ppt.mjs`、`node tools/task_spec.mjs`、`node tools/test_preview_state.mjs` 通过。
- 演示文稿环境的 `slides_test.py` 因缺少 `pdf2image` 无法运行；已用 PPTX XML、压缩包完整性、输出文件访问和在线预览访问完成替代检查，后续生产环境需补齐渲染依赖。

当时遗留的问题已在后续迭代解决：客户案例/客户专属数据现在进入逐页 change-plan，销售可以选择保留、改写、待确认或删除，并通过确认后重跑执行；新客户事实仍必须由资料或证据支持。

# 2026-08-23 · 客户化改写逐页确认闭环

- 新增 `PATCH /api/customer-rewrite/:id/plan`（同时兼容任务 ID 裸路径），支持提交 `pages` 数组或单页 `page/action`，动作严格限制为 `retain`、`rewrite`、`pending`、`remove`，并持久化 `planVersion`、`planConfirmed`。
- 新增 `POST /api/customer-rewrite/:id/rerun`。接口只复用服务端保存的源 PPT 副本和目标客户配置，在确认计划后重新生成，不覆盖上传源文件；重跑期间继续使用原任务轮询和日志链路。
- 执行器 `tools/customize_ppt.mjs` 接收服务端 change-plan。`remove` 会从正常可编辑 PPTX 中删除对应页面，`retain` 不替换页面文字，`rewrite` 才执行客户名称替换；输出仍保留母版、版式、图片和可编辑文本结构。
- 前端逐页变更计划改为可编辑下拉框，提供“保存计划”和“保存并重新生成”；任务状态区分处理中、过程稿、待确认和可交付，并保留品牌图片质量门禁。目标客户 Logo 可随上传任务提交，重跑时替换源客户 Logo；未提供时移除源 Logo 并停留在待确认/过程稿。
- 回归：4 页源 PPT 计划删除第 3 页后重跑，输出 PPTX 为 3 页，压缩包内 3 个 `ppt/slides/slide*.xml`，质量门禁通过；非法页码返回 400。通过 `node --check server.mjs app.js tools/customize_ppt.mjs`、`node tools/task_spec.mjs`、`node tools/test_preview_state.mjs`。
### 2026-08-23：客户化改写品牌素材门禁

- 修复客户化改稿只替换文本、保留源客户 Logo 的质量缺陷。`tools/customize_ppt.mjs` 现在读取 artifact-tool 的 `slide.images`，记录媒体引用、对象名和原始框位；首屏顶部紧凑品牌图、同源复用图和显式 Logo 对象会进入品牌素材清单。
- 未上传目标客户 Logo 时，从输出副本删除识别到的源客户品牌图，封面逐页计划改为“待确认”，并标记缺少“目标客户 Logo/品牌素材”；不会把带有中国移动 Logo 的文件标成可交付。
- 客户化改写上传支持可选的 PNG/JPG/SVG/WEBP 目标 Logo；服务端保存到任务私有临时目录，并在保持原始框位、裁剪和可编辑 PPTX 的前提下替换图片。
- 新增 `tools/check_customer_rewrite.mjs`，校验 PPTX 页数、文本残留、替换失败、品牌素材状态、待确认页，并输出 `.quality.json`。服务器在生成在线预览前运行该门禁，任务状态为“待确认”或“可交付”而不是笼统“已完成”。
- 回归：`中国移动-分贝通企业支出管理解决方案V4.pptx` 106 页。无目标 Logo 输出中源客户文字残留 0、疑似源 Logo 1 个且已删除，质量状态 `needs-review`；提供 Logo 输出中疑似品牌图 1/1 已替换，PPTX ZIP 完整且仍为可编辑结构，剩余 14 页待资料确认。

### 2026-08-23：客户化改写最终回归与门禁加固

- 固定回归场景为“中国移动 106 页方案 → 可口可乐”。重新执行当前版本后确认：106 页可编辑 PPTX、1501 个文本对象、645 个图片对象；XML 中“中国移动”残留 0，封面标题为“可口可乐”。
- 无目标 Logo 时，封面源品牌图 1 个被删除，质量状态 `needs-review`；提供目标 Logo 时，品牌图 1/1 在原框位替换。
- 质量检查新增 PPTX ZIP 完整性、实际幻灯片数、可编辑文本对象和空标题/页码占位页检查。当前基准识别到 105 页含可编辑文本对象，以及 2 个空白/页码占位页，因此即使人工把所有动作改为保留，也不会绕过交付门禁。
- 服务端读取质量报告并把 `zipIntegrity`、`editablePptx`、`editableShapeCount`、`placeholderPageCount` 和具体问题写入任务；前端在待确认状态直接展示对应问题。
- 回归同时覆盖逐页动作校验和确认后重跑：非法页码返回 400；4 页样本删除第 3 页后输出 3 页正常可编辑 PPTX。

### 2026-08-23：历史客户化任务兼容归一化

- 新增 `tools/customer_rewrite_compat.mjs`，在 `/api/customer-rewrite` 读取历史任务时补齐 `deliveryStatus`、`quality/qualityGate`、`planVersion`、指标对象、计划数组、质量路径和 Logo 字段。
- 兼容旧状态别名：`done/success` 归一为 `completed`，`running` 归一为 `processing`，`pending` 归一为 `queued`。历史已完成但没有质量门禁的任务保守归一为 `deliveryStatus=process`，不会误标为可交付。
- 归一化只发生在内存读取边界，不会在服务启动或 GET 请求中重写 `.tmp/customer-rewrite-tasks.json`。更新任务时仍沿用现有持久化流程。
- 新增 `tools/test_customer_rewrite_compat.mjs`，验证旧任务字段缺失、状态别名、质量字段兼容和输入对象不变。
- API 回归：旧任务 GET 返回 `deliveryStatus=process`、`quality=null`、`qualityGate=null`、`planVersion=0`，原任务文件 SHA-256 前后一致。

### 2026-08-23：客户资料包与来源绑定

- 客户化改写任务支持附加最多 20 份资料文件，包含 PDF/Word/Excel、文本/Markdown/CSV 和品牌图片；单文件上限 25 MB。
- 服务端将资料保存到 `.tmp/customer-rewrite-materials/<task-id>/` 私有目录，任务 JSON 记录 `materials` 的文件名、类型、大小、来源、路径和创建时间，同时记录源 PPT 的 `source` 元数据。
- 前端客户化改写入口新增“客户资料包”多选上传。第一版只完成来源绑定和追溯，不读取附件内容，也不会把附件未经确认伪造成目标客户事实。
- 回归：multipart 上传源 PPT + `客户资料.txt` 返回 202，任务包含 `source.type=upload` 和 `materials[0].kind=text`，磁盘私有文件存在；`node --check server.mjs app.js` 与 `node tools/test_customer_rewrite_compat.mjs` 通过。

### 2026-08-23：客户资料证据抽取最小闭环

- 新增 `tools/extract_customer_materials.mjs`。客户化改写任务在页面结构提取后、逐页规划前抽取资料；TXT/MD 按段落和行号、CSV 按行生成证据片段，保留来源文件、私有路径、行范围、截断状态和抽取时间。
- 每份资料在任务私有目录生成独立 `material-extract-<material-id>.json`，并生成汇总 `material-extract.json` 与任务证据文件 `evidence.json`。每条证据包含文件 SHA-256/版本，任务新增 `evidence`、`materialEvidence`、`materialExtraction` 和按实际证据推导的 `missingInputs`；材料元数据补充抽取状态、证据数量、抽取文件和错误。
- PDF/Word/Excel/图片第一版只标记 `unsupported`，不读取、不猜测内容，也不会因为文件已上传就把其中可能存在的客户事实视为证据。没有证据时，行业背景、客户现状/痛点、案例/数据仍保持缺失；客户名称和 Logo 的既有安全替换规则不变。
- 新增 `tools/test_extract_customer_materials.mjs`，覆盖 TXT、CSV、未支持 PDF、文本行号、CSV 行号、独立/汇总 JSON、缺失项推导和无证据边界；历史任务兼容测试补充新字段默认值。
- HTTP 回归：上传 1 份 PPTX 和 1 份 TXT 后，任务生成 2 条带文件哈希与行范围的证据，`materialExtraction.status=completed`；资料已覆盖行业、痛点和案例数据，因此只保留“客户 Logo/品牌素材”为缺失项。任务继续生成可编辑 PPTX 并进入既有质量门禁，不因新增抽取阶段中断。

### 2026-08-23：客户化改写前端主流程回归

- 通过真实 multipart 请求回归客户化改写入口：上传源 PPT、目标客户 Logo 和多份客户资料可创建异步任务；任务 `rewrite-1787474217505-fe44cd2b` 最终进入 `review`，生成 2 页改写前后缩略图，质量门禁确认输出为可编辑 PPTX。
- 前端已核对客户化改写弹窗、Logo 上传、资料包多选、逐页动作、前后缩略图对照、计划保存及重新生成所需 DOM 和事件绑定。
- 修复窄屏逐页计划布局：页面详情、前后对照、动作选择和原因分别占用独立网格行，避免动作下拉框与缩略图重叠；前后大图对照在窄屏改为单列。
- 客户化任务列表在窄屏限制为自身区域横向滚动，避免 800px 最小宽度把整个页面撑出视口。
- 检查通过：`node --check app.js server.mjs tools/customer_rewrite_compat.mjs tools/customize_ppt.mjs tools/check_customer_rewrite.mjs`；`GET /api/customer-rewrite?limit=20` 正常返回新旧任务、资料、逐页缩略图与质量门禁字段。
- 当前环境的应用内浏览器不可用，因此本轮没有完成真实浏览器截图和点击级视觉验证；已完成 API 真实上传、任务轮询、静态 DOM/CSS 和 JavaScript 语法回归，后续需用可用浏览器补一轮桌面与窄屏截图确认。

### 2026-08-23：模板契约 v1

- 新增 `tools/template_contract.mjs`，从 `data/pages.json` 与 `data/source-metadata.json` 生成 `data/template-contract.json`，为 550 个页面保留 `pageId`、源文件/源页、页面角色、标题槽位、正文槽位、文本容量、旧文案 SHA-256 指纹、必须保留对象和可编辑约束。
- 新增 `readTemplateContract`、`findTemplatePage` 与 `validateTemplateContent`，生成单页 PPT 时可通过 `--contract` 按页面 ID 或源文件/源页读取契约并在写入前检查标题/正文容量。
- `tools/generate_page_ppt.mjs` 支持可选 `--contract`，不传时保持旧行为兼容；契约校验失败会在导出前明确报错。
- 新增 `tools/test_template_contract.mjs`，覆盖契约生成、源页定位、槽位指纹与容量限制；已通过 `node tools/test_template_contract.mjs`、`node --check tools/template_contract.mjs tools/generate_page_ppt.mjs`。
# 2026-08-23 · 客户化改稿场景收口：证据、版本与对话入口

本轮目标：覆盖销售上传已有客户 PPT 并改成目标客户的真实工作流，例如中国移动方案改为可口可乐方案；不把它简化为全文替换客户名。

完成事项：

- 客户资料包支持 TXT、MD、CSV 的安全证据抽取，保留来源文件、私有路径、行号、SHA-256 哈希和版本；PDF、Word、Excel、图片暂标记未抽取，不把未知内容写入 PPT。
- 客户化任务新增计划版本、输出版本和操作日志，记录任务创建、逐页计划确认和输出生成；旧任务通过兼容层自动补齐默认字段。
- 左侧对话新增源 PPT 附件入口。附加 PPT 后发送“改成/改为/面向某客户”的描述，会直接创建异步客户化改写任务；普通聊天和普通 PPT 工作流不受影响。
- 逐页计划摘要展示资料证据数量、证据文件和计划/输出版本，销售能追溯当前稿基于哪批材料生成。

验证：

- `node --check server.mjs app.js tools/extract_customer_materials.mjs tools/customer_rewrite_compat.mjs`
- `node tools/test_extract_customer_materials.mjs`
- `node tools/test_customer_rewrite_compat.mjs`
- `node tools/test_preview_state.mjs`
- `node tools/task_spec.mjs`
- HTTP 端到端上传源 PPT 加客户 TXT：资料抽取完成，生成 1 条带行号和哈希的证据；输出为正常可编辑 PPTX，质量门禁按缺 Logo/待确认保持过程稿状态。

残余风险：当前环境没有可用的应用内浏览器会话，尚未完成桌面和窄屏的真实截图点击回归；已完成静态 DOM/CSS、HTTP、PPTX XML、渲染和脚本检查。

### 2026-08-23：证据驱动的客户化缺口页规划

- 新增独立工具 `tools/plan_customer_gap_pages.mjs`，输入 `changePlan`、`evidence`、`missingInputs`，输出 `gapPagePlan`，固定覆盖客户背景、客户现状与痛点、客户案例与数据、下一步推进计划四类页面。
- 规划器会关联现有逐页计划：已有对应源页面时输出 `rewrite`，没有对应源页面时输出 `create`。每个可生成页面必须带 `evidenceIds`，事实草稿只原样复制证据片段，不自行扩写或推断。
- 没有匹配证据，或 `missingInputs` 仍明确标记缺少该类信息时，页面只输出 `pending`、原因和所需资料；不会出现 `factDrafts` 或其他事实内容。即使已有部分证据但缺失项尚未解除，也只保留证据 ID，继续等待人工确认。
- 新增 `tools/test_plan_customer_gap_pages.mjs`，覆盖无证据四类全部待补、已有源页的证据改写、无源页的新建、证据 ID 绑定，以及 `missingInputs` 对事实输出的强制阻断。

### 2026-08-24：客户化缺口页、输出映射与大方案安全回归

- 修复客户化任务状态字段混用：`changePlan` 始终保存源 PPT 的逐页动作，`outputPlan` 保存包含新增页的最终输出顺序；新增页使用 `sourcePage=null`，并保留 `gapPageId`、`generatedPageId` 和 `evidenceIds`。
- 修复任务最终状态漏写 `evidence`、`gapPagePlan`、`generatedGapPages` 和 `outputPlan` 的问题；历史任务兼容层会把旧的含 `create` 输出计划还原为源页计划，避免确认后重跑被错误拒绝。
- 缺口页生成只使用被标记为 `rewrite` 的源页作为改写关联；`pending` 或 `remove` 页面不会被当作安全模板事实来源。没有源页时按公司模板新建页面，并确保新增页插入尾页之前。
- `tools/generate_page_ppt.mjs` 在写入 1 至 3 个正文槽后清理未使用的旧模板长文案；保留旧文案质量断言，避免模型返回短内容时把源模板内容残留到新增页。
- 新增客户事实门禁：源客户长段文案被直接替换为目标客户名称、但没有逐页证据时，质量报告标记 `unsupported-customer-rewrite` 为 blocker。文本替换成功不再等价于事实改写完成。
- 无客户资料时跳过 106 页逐页模型分类，直接采用确定性计划；只有存在目标客户证据时才调用 DeepSeek 分类和重试，降低大方案等待时间并避免无证据猜测。

回归结果：

- 测试客户竞品样例：14 页源稿，输出 18 页；4 页新增缺口页全部绑定证据，PPTX ZIP 完整、可编辑对象存在；质量门禁正确停在 review，拦截源客户残留和无证据长段改写。
- 中国移动 → 可口可乐真实大方案：106 页输入，106 页输出，0 页无证据新增客户页，客户背景/现状与痛点/案例数据/下一步四类缺口均为 pending；源 Logo、占位标题和重复标题被质量门禁标记，任务不伪装为可交付。
- 任务日志保留模型 JSON 重试和批次降级记录；源 PPT 未被修改。当前渲染仍受环境缺少 `pdf2image` 影响，已用 `unzip`、PPTX XML 可编辑性、页数映射和 API 轮询完成替代验证。

## 2026-08-24 · 指定页面引用改写闭环

- 资源库卡片新增右键菜单“添加到对话”。当前 AI 会话持久化 `referencePageId`，旧会话缺失该字段时自动按空引用兼容。
- 左侧对话区新增引用页面缩略图、标题、唯一 ID 和移除引用按钮。引用只在用户主动移除后清除，原页面不会被修改。
- 对话请求携带 `referencePageId` 后，服务端跳过模糊意图识别，进入确定性的 `modify_page` 工作流：校验页面来源、复用真实源 PPT 模板、结合原始文本和用户描述调用 DeepSeek 生成 JSON，再导出可编辑 PPTX。
- 新页面标记为 `sourceType=ai-generated`、`generationMode=reference-page`，并记录 `sourceReferencePageId`、`sourceReferencePageTitle`、模板来源和生成尝试次数；生成页自动追加到当前预览队列，原页面继续保留。
- Agent 流程日志新增 `reference-page-start`、`reference-page-selected`、`reference-page-generated`、`reference-page-added-to-preview` 等节点，便于排查“引用了哪页、使用了哪个模板、生成了哪页”。
- 回归：`node --check app.js`、`node --check server.mjs`、`node tools/task_spec.mjs` 及现有 `tools/test_*.mjs` 全部通过；隔离端口验证无效引用会在模型调用前返回明确错误。

资源库分页评估：当前约 550 个页面，局域网本机部署短期仍可承受首屏加载，但所有页面同时加载缩略图会放大内存、网络和 DOM 成本。下一阶段应把资源库改为服务端分页接口，搜索、结构标签、场景标签、状态筛选一起下沉到服务端，默认每页 12 条并对缩略图懒加载；预览队列和引用页面不受分页影响。先不在本轮与引用改写一起改动，避免引入跨功能回归。

### 2026-08-24 · 指定页请求未生效排查

- 用户实际请求对应的 `.tmp/agent-flow.jsonl` 记录中没有 `referencePageId`，任务被旧服务路由为普通资源库推荐，并未进入指定页改写流程。
- 根因是 4312 端口仍运行修改前的 `server.mjs` 进程；代码已完成但服务未重启，前端发出的引用参数没有被旧进程处理。
- 已只重启 PPT Master 的 4312 服务，未终止其他端口服务。隔离回归确认同一请求会输出 `operation=modify_page`、`action=reference_page`，无效引用在模型调用前明确失败。

### 2026-08-24 · 资源库服务端分页

- 新增 `GET /api/pages`，支持 `page`、`pageSize`、`query/q`、`structure`、`scene`、`status` 和 `ids` 参数；默认每页 12 条，服务端完成搜索、标签和标注状态筛选。
- 返回当前页数据、总数、页数、状态统计和可用标签枚举。资源库页面移除全量 `data/pages.js` 加载，改为当前页渲染并提供上一页/下一页。
- 详情抽屉、预览队列、在线预览和 AI 引用页在当前页之外时按 ID 补取，不因分页丢失既有工作流。
- 新增 `tools/test_library_pagination.mjs`，覆盖分页数量、筛选结果、状态统计和按 ID 查询；服务端分页专项回归通过。
- 根据实际缩略图加载速度，将前端和服务端默认分页从 48 调整为 12；当前首屏接口返回 12 页，339 个有效页面共 29 页。
- 变更计划弹窗新增质量门禁原因摘要，把 blocker/review 转成可读的处理提示，销售无需先读质量 JSON 才能知道该补资料、上传 Logo 或调整页面动作。
### 2026-08-24：引用页面改写问题修复

- 引用页面工作流改为独立短流程：只读取引用页面和原始模板，不再展示无关的当前预览/公司规则步骤，也不会无条件检索飞书资料；步骤标题按引用任务展示。
- 普通工作流的上下文步骤也按任务类型动态命名：修改预览读取当前预览，单页/整套生成读取公司模板和资源库，复用/追加读取资源库页面，避免所有任务显示同一套固定标题。
- 修复标题-only 页面生成失败：`deck-10-p001` 只有一个标题文本框时跳过正文生成，按原标题结构确定性替换目标竞品名称，正文为空，继续使用真实源 PPT 模板导出可编辑 PPTX。
- 生成页记录 `sourceModel=deterministic-title-only`、`generationMode=reference-page-title-only`，保留引用页、模板页和任务日志关联。
- 引用状态改为“待发送引用 + 消息级引用”：引用缩略图显示在输入框上方，发送后写入具体用户消息并清空待发送状态；同一会话后续消息可引用不同页面。
- 回归：实际请求“改成差旅纵横进行对比”成功生成“分贝通 vs 差旅纵横对比优势”，`body=[]`，PPTX XML 包含新标题且不包含“易快报”；语法、任务规格和分页测试通过。

### 2026-08-25：无引用页面的误路由保护

- 排查最近对话发现，“标题改一下，跟航旅纵横的对比”没有携带 `referencePageId`，却被模型路由为资源库推荐，导致一次性选中 10 页；该任务日志为 `agent-task-1787557254406-1a6033`，不是 PPT 生成失败，而是目标页面缺失时发生了错误的预览变更。
- 新增安全门：标题/页面修改类请求在没有引用页面、且没有当前预览目标时，不再读取资源库、检索飞书或更新预览，只提示先右键“添加到对话”。
- 请求日志现在记录 `referencePageId` 和 `draftIds`，后续可直接确认前端是否把引用绑定到该条消息。
- 回归：无引用的标题修改请求只返回澄清提示，预览保持不变；已有引用的“改成差旅纵横进行对比”继续走标题-only 可编辑 PPTX 流程。

### 2026-08-27：引用页生成失败不再伪装成功

- 排查任务 `agent-task-1787811888263-c13f00`：用户要求修改 `deck-11-p003` 的标题和正文，但 DeepSeek 连续 3 次返回 `finish_reason=length` 且 `message.content` 为空。
- 根因是单页生成默认 `max_tokens=900`，模型输出预算被耗尽后，旧逻辑把原页面的“产品定位 / 整体解决方案 / 分贝通”当作正文兜底，并继续导出、入预览和标记完成。
- 单页生成预算改为环境变量 `AGENT_PAGE_MAX_TOKENS`，默认 1800；日志额外记录 `finishReason` 和 reasoning 摘要。
- 引用页正文改写禁止使用通用原文兜底；模型失败、正文只返回原栏目名、标题未缩短或正文未达到扩充要求时，任务直接失败，原页面、预览队列和生成页面均不变。
- 已撤回该次错误生成页 `ai-page-1787811950133-3b9232`，保留失败日志和原始产物用于追溯。
- 实际回归：同一请求成功生成“一体化企业支出管理平台”，正文包含两条完整业务说明，导出 PPTX 可解压且文本对象保持可编辑；`tools/test_reference_page_validation.mjs` 覆盖失败门禁。

### 2026-08-27：公开仓库初始化准备

- 初始化 Git `main` 分支，新增 MIT License、`package.json`、工作区初始化脚本和公开版 README。
- `source_ppts/`、逐页截图、企业索引、公司知识、飞书资料、AI 日志、会话、导出文件和 `.env` 均加入忽略规则；公开仓库只保留代码、工具、文档和空目录占位文件。
- PPT 工具不再写死本机 Codex runtime 路径，统一通过 `tools/presentation_runtime.mjs` 查找，可使用 `PRESENTATIONS_ARTIFACT_UTILS` 或当前用户的 Codex runtime。
