# 开发计划、PMF 与迭代路线

> 当前实现以 `DEVELOPMENT_LOG.md` 和 `PPT_KNOWLEDGE.md` 为准。原计划中的图片型 PPT 输出、置信度字段和资源库手动预标注入口已被后续决策覆盖。

## PMF 假设

销售愿意上传历史 PPT 并校正少量 AI 标签，只要页面库能让新方案的首稿时间从半天降低到 1 小时以内，并且保留人工对叙事和品牌的控制。

## 产品最终目标（2026-08-23 重划基线）

PPT Master 的最终目标不是“聊天后自动拼出几张相关页面”，而是一个以公司现有 PPT 和飞书资料为知识底座的销售方案生产系统：销售从对话、录音或明确需求开始，系统先理解任务并形成完整页面计划，再从页面资产库中选择可复用页面；缺口页面基于公司模板和有来源的事实生成；销售可以在在线预览中调整叙事、顺序和内容，最后导出一份可编辑、可追溯、可对客使用的 PPT。

这个目标包含四个不可牺牲的约束：

- 叙事由任务计划控制，页面相关不等于页面应该出现；每页必须有角色、目的、前后依赖和处理方式。
- 用户始终保留控制权。系统可以自动完成低风险的新增和推荐，但修改、删除、替换和整套方案必须只作用于明确目标，并能显示变更前后差异。
- 所有页面都必须沿用公司真实模板和品牌规范。AI 负责理解、检索、改写和补缺，不得凭空设计一套通用 PPT。
- 所有事实、模板来源、生成版本和人工修改都可追溯；质量不通过时宁可停在草稿或报告缺口，也不能把错误页面伪装成完成结果。

## 核心场景：客户化方案改写

除从零生成和资源库组合外，系统必须支持销售上传一份已有客户方案，再将它改写为面向另一位客户的方案。例如上传“中国移动-分贝通企业支出管理解决方案”，要求改写为“面向可口可乐”的客户方案。

该流程不是全文替换客户名称，而是按页面处理：

1. 保留：公司介绍、通用产品能力、服务能力、已验证的通用价值页，保持原有版式和可编辑元素。
2. 改写：封面、目录、客户背景、方案定位、价值主张、章节标题和与客户名称有关的文字。
3. 删除或降级：原客户案例、原客户数据、原客户组织/系统/接口、专属交付承诺及无法验证的数字。
4. 新建缺口：新客户的行业背景、业务痛点、案例、数据、Logo、组织名称或定制方案；只有取得销售输入、飞书资料或可验证公开来源后才生成。
5. 生成变更清单：逐页列出保留、改写、删除、新建、待补充及事实来源，销售可在执行前审核。

输出必须是源 PPT 的可编辑副本，保留其母版、版式、图片、图表和可复用元素；系统不得将源文件扁平化为图片，也不得把原客户事实带入新客户方案。

### 当前实现状态（2026-08-23）

客户化改写已经完成第一版可执行闭环：上传、异步任务、源客户识别、逐页 change-plan、销售确认、副本改写、删除页面、目标 Logo 替换、在线预览、质量门禁和可编辑 PPTX 下载均已接入。第一版只自动替换明确的源客户名称；原客户案例、客户数据、组织/接口和专属承诺会进入待确认计划，不会静默改成目标客户事实。

## 当前是否偏离

当前系统已经完成了页面级资产库、PPT 导入拆页、标题/正文/标签元数据、异步导入、飞书知识同步、基础意图识别、流式 Agent 和可编辑 PPTX 导出。这些是最终目标的底座，方向没有偏离。

但对话生成部分已经超出了“可控 MVP”应有的边界，存在明确偏差：

- 任务理解、页面数量和“预览第 N 页”的解析仍混在同一套页数启发式中，导致“第二页”被误解为“两页”。
- 前端本地 draftIds 事实上承担了任务状态，服务端没有保存任务对应的页面队列、版本和操作历史，导致局部修改可能替换整组页面。
- 资源库匹配分数直接决定“直接复用”，没有把“整合、重组、只保留、不要其他内容”识别为需要改写或新建。
- 页面生成仍依赖少量模板特例和文本框启发式，尚未形成可复用的页面角色/槽位契约。
- 导出成功目前不等于内容正确；事实、必选项、排除项、页面角色和视觉溢出还没有统一成为生成门禁。
- 计划中的“HTML 先行、PPT 图片打包”已被用户明确否定，当前产品目标应以“HTML 在线预览 + 正常可编辑 PPTX 导出”为准。

因此当前阶段不应继续扩充模板特例或外部工具数量，应该先把对话到预览的控制链路修正，再继续增加生成能力。

## 重新划定的下一步工作计划

### P0：冻结范围与验收基线（半天）

- 暂停新增模板特例、长方案自动生成和更多外部工具接入。
- 建立一组固定回归任务，每条包含用户原话、预览初始队列、期望操作、期望页面数量、期望保留/替换的页面 ID。
- 将“生成成功”改定义为：任务规格正确、页面计划正确、队列变更正确、内容门禁通过、文件可编辑且可追溯。
- 增加客户化改写基准：以中国移动方案为源，目标客户为可口可乐；验收源文件保真、原客户事实隔离、待补充内容显式列出和输出 PPTX 可编辑。

### P1：任务规格和预览状态（优先实现）

- 新增机器可读 `task-spec`：操作类型、目标页面、目标数量、保留规则、必须包含、明确排除、标题动作、生成方式。
- 服务端保存每个会话的权威预览队列、顺序、版本和操作日志；前端只展示服务端确认后的结果。
- 明确区分“第二页”和“两页 PPT”：只有“做/生成/输出 N 页”才设置目标数量；“第 N 页/预览第 N 页”只设置目标索引。
- 对替换、删除、调序增加硬校验：非目标页 ID 和顺序不变，目标页位置不变，页面数量符合任务规格。

### P2：计划与变更执行分离

- Agent 先输出简短的任务理解和页面变更计划，再执行可验证的操作。
- 新建一页默认追加到当前预览；明确修改第 N 页只替换该页；明确“直接复用”才允许不改内容直接入队。
- “整合、重组、只保留、不要其他内容、标题改为”默认进入调整生成，不得被资源库高分直接判为复用。
- 低风险新增可以自动执行；替换/删除/整套替换必须先展示变更摘要，执行后回传前后队列差异。
- 客户化改写先生成逐页 change-plan，再对已确认可改写页面生成副本；无法验证的新客户信息标记为待补充，不能静默替换。

当前客户化改写已完成 P2 的第一版基础执行器和逐页确认重跑，并已补齐逐页前后缩略图对照与客户资料包来源绑定。资料包目前只保存来源，不自动读取或把附件内容写成客户事实；新建缺口页和资料驱动的内容改写仍属于后续迭代。

### P3：页面角色与模板槽位契约

- 为高频页面建立结构化模板契约：页面角色、适用主题、禁用主题、标题/副标题/模块/图片槽、文本容量和可编辑性。
- 生成内容先填充页面契约，再由导出器写入继承的文本框和图片框；不再用“最长的三个文本框”猜正文。
- 第一批只覆盖销售最常用的 10-20 个页面角色：封面、目录、产品总览、能力对比、流程、价值总结、案例、服务、章节页和尾页。

### P4：语义、事实和视觉质量门禁

- 必须项、排除项、标题、页面角色和模板旧文案逐项校验。
- 页面事实必须关联 PPT 或飞书文档证据；没有证据的数字、客户名称和承诺直接阻断。
- 检查文本溢出、未填充占位符、残留旧文案、重复页面和页面角色跳跃。
- 任一门禁失败时不更新预览，返回失败原因和可执行的修复建议。
- 客户化改写额外检查源客户名称、客户 Logo、客户案例、客户数据和专属接口是否仍残留；明确保留的通用分贝通信息不应误删。

客户化改写已完成源客户名称残留检查和可编辑 PPTX 完整性检查；Logo、图片内文字、图表数据和源客户专属事实的视觉级检查仍需在渲染依赖补齐后完成。

### P5：真实销售验证与 PMF 判断

- 用 10-15 条真实对话覆盖新建、整合、局部修改、删除、调序、直接复用和整套方案。
- 记录任务规格准确率、局部修改保持率、页面计划采纳率、人工修改次数、首稿耗时和导出成功率。
- 只有当销售能在 1 小时内完成一份可对客过程稿，且不需要反复纠正页面范围和顺序，才进入更大规模的整套生成和多人线上化。
- 新增验收：销售上传一份客户方案后，能在不破坏原模板和页面结构的前提下得到一份客户化过程稿，并清楚看到仍需补充的客户资料。

### 暂缓事项

在 P1-P4 完成前，暂缓继续扩充外部搜索工具、复杂多 Agent 协作、自动生成超长整套方案和新的视觉模板。它们会放大当前的状态和质量问题，而不会解决根因。

## 首个验证指标

- 1 位销售完成 1 份素材上传、标注和组合闭环。
- 组合一份新方案耗时 ≤ 60 分钟。
- ≥ 80% 页面被认为“可复用”或“只需轻微修改”。
- 生成的过程稿中，页面来源可追溯率 100%。
- 一周内至少完成 3 次真实组合，而不是只浏览页面库。

## 继续 / 调整判断

- **继续**：组合耗时达标，且销售愿意重复使用页面库。
- **调整**：页面可复用率低，优先改标签体系和页面颗粒度，不先换模型。
- **暂停**：销售仍偏好从空白 PPT 开始，说明页面级复用价值不足，需要重新验证场景。

## 分阶段计划

### Phase 0：样本与规则（1-2 天）

- 收集 1 份销售最常用 PPT（10-15 页）。
- 确认公司品牌规范和最终稿样例。
- 手动定义页面类型、场景和弃用规则。
- 产出 20-30 页人工基准标签，作为 AI 评估集。

### Phase 1：MVP 闭环（约 1 周）

- 上传与文件校验。
- PPT 转图片 / 文本的页面级解析服务。
- DeepSeek v4 Flash 预标注接口，支持重试和人工覆盖。
- 页面库检索、详情、状态切换和来源追溯。（当前已先完成真实 247 页索引）
- 拖拽或按钮式页面组合。
- HTML 预览和过程稿保存。
- 最小日志：解析耗时、AI 耗时、人工修改次数、页面复用次数。

### Phase 1.5：公司 PPT 知识基线（当前阶段）

- 从所有来源 PPT 提取整套页面顺序和场景转场统计。
- 提取页面密度、文本框、图片、标题来源和版式签名。
- 提取版式尺寸、布局名称、字体、字号和颜色等品牌证据。
- 形成可读的 `PPT_KNOWLEDGE.md` 和机器可读的 `data/ppt-patterns.json`。
- 将统计结果转成 Agent 的“先提纲、再选页、后预览”约束，不直接当成固定模板。
- 用真实销售任务验证：AI 推荐页面是否比纯关键词筛选更快、更符合公司叙事。

### Phase 1.6：公司知识与方案规划层（下一优先级）

当前单页和组合结果出现“内容相关但前后无逻辑”的根因，不是单纯模型能力不足，而是缺少生成前的任务规划层。页面匹配不能直接等同于方案生成，必须先得到可验证的页面槽位和叙事依赖。

#### 目标分层

1. **任务理解**：由模型识别普通问答、选页调整、单页生成、连续多页生成和完整方案，并保留用户消息中的多个目标、页数、每页用途和修正意见。
2. **公司知识**：导入飞书产品文档、模块介绍、服务与 SLA、案例资料和销售口径，按文档版本保存来源；不把未经来源支持的事实交给模型自由发挥。
3. **方案规划**：先生成章节、页面角色、顺序、页面之间的论证关系和缺口，再为每个页面槽位检索资源库页面或标记需要新建。
4. **页面生成**：只对缺失槽位生成页面，必须继承指定模板，并只能使用任务上下文和公司知识中的事实。
5. **质量校验**：检查封面/目录/章节/内容/尾页完整性，检查相邻页面重复、章节跳跃、事实来源和模板适配；失败则回到规划层重排，不直接把低质量结果放入预览。

#### 飞书知识接入

- 使用 `lark-cli docs +fetch` 读取飞书文档，不在浏览器直接访问文档。
- 保存 `docToken`、标题、revisionId、抓取时间和文档类型；内容按章节切分，建立术语、产品能力、适用场景、事实声明、禁止承诺和来源引用。
- 首批优先接入：公司/产品总览、商旅/费控/AI Agent 模块介绍、服务与 SLA、客户案例和标准销售话术。
- 文档更新后按 revisionId 增量重建知识索引，旧版本保留用于历史任务追溯。

#### 机器可读产物

- `data/company-knowledge.json`：事实、术语、能力边界、来源和版本。
- `data/narrative-rules.json`：不同方案类型的章节顺序、必需角色、可选角色、前置依赖和页面数量范围。
- `data/source-registry.json`：PPT 与飞书文档的来源、版本、抓取状态和更新时间。
- 每次任务保存 `task-spec`、`plan`、`evidence`、`validation`，与生成页面和导出文件关联。

#### 验收标准

- 用户一句话包含多个目标时，不丢失后半段要求；例如“先做一页总览，后面每个产品各做一页”必须识别为页面系列，而不是单页。
- 生成前能看到完整页面计划；计划必须说明每页用途、来源页面或“需要新建”的原因。
- 任何页面的数字、客户名称、能力边界都能追溯到 PPT 或飞书文档来源。
- 结构校验失败时不自动加入预览，返回缺口和下一步动作。

### Phase 2：可用性提升（2-3 周）

- 基于内容和标签的混合检索。
- 页面相似度去重和重复页提醒。
- 去重只输出候选，不自动删除；人工确认后再合并来源和使用统计。
- 批量确认、快捷键、标注模板。
- 页面使用反馈：适用 / 不适用及原因。
- HTML 导出为图片型 PPT，先满足不可编辑交付。

### Phase 3：生产化（4-6 周）

- 任务队列、断点续传和失败重跑。
- 飞书文档 / 云盘存储接入。
- 权限、审计、版本和回滚。
- 可编辑 PPT 导出评估：仅在销售明确需要二次编辑时投入。
- 基于停留时长、复用率和人工反馈的素材排序。

## 技术选择

- 前端：React 或 Next.js 工作台；当前原型先用原生 HTML/CSS/JS，避免过早绑定框架。
- PPT 解析：服务端优先使用 LibreOffice headless 转 PDF / 图片，并用 `python-pptx` 提取文本；复杂图形保留整页截图。
- AI：OpenAI 兼容协议适配器，默认模型名 `deepseek-v4-flash`，Key 只放服务端环境变量。
- 存储：MVP 可用 SQLite + 对象存储；生产再迁移到 PostgreSQL 和飞书云盘。
- 输出：在线预览与 PPT 导出双形态；当前导出器按页面顺序合并原始 PPTX 和 AI 生成的可编辑 PPTX，保留文本、图片和版式编辑能力。

## 主要风险与处理

- **解析质量不稳定**：整页截图作为保底，文本和图片提取失败不阻塞页面入库。
- **AI 标签不可靠**：置信度低于阈值自动进入待确认，不自动进入在库。
- **页面颗粒度不合适**：用 1 篇 PoC 和 1 小时组合指标验证，不提前拆到元素级。
- **PPT 与 HTML 不一致**：在线预览使用原始页面渲染图，导出使用原始/模板 PPTX 合并；后续需要继续验证复杂母版、嵌入字体和动画的保真度。
- **敏感材料泄漏**：文件和 API 调用全部服务端处理，日志不记录原文内容。

## 2026-08-23 · 客户化改稿与对话入口收口

- 客户资料包已完成最小安全证据抽取：TXT、MD 按段落和行号记录，CSV 按行记录；每条证据带来源文件、私有路径、SHA-256 哈希和版本。PDF、Word、Excel、图片暂不读取，只标记未支持，避免把附件内容伪造成客户事实。
- 客户化任务新增 `planVersion`、`outputVersion` 和最多 100 条 `operationLog`。计划确认、输出生成、任务创建都会留下事件，前端逐页计划摘要显示计划/输出版本和资料证据数量。
- 左侧 AI 对话新增源 PPT 附件入口。用户附加 PPT 并发送“改成/改为/面向某客户”的描述时，直接创建 `/api/customer-rewrite` 异步任务；普通聊天、资源库推荐和单页生成仍走原有工作流。
- 真实回归：`regression-single-page.pptx + 客户资料.txt` 在 4321 端口上传，资料抽取完成并产生 1 条证据，输出版本为 1，PPTX 可编辑，任务按质量门禁保留为过程稿/待确认。

## 2026-08-24 · 指定页面改写与资源库性能计划

- 支持从资源库卡片右键“添加到对话”，对话区展示引用页；用户描述后基于该页真实 PPT 模板生成一张新的可编辑页面，原页保持不变并自动追加到预览。
- 引用页生成使用确定性路由，不依赖关键词或模型意图猜测；生成页、引用页、模板页和任务日志互相可追溯。
- 资源库性能下一步采用服务端分页：`GET /api/pages?page=1&pageSize=12&query=&structure=&scene=&status=`，筛选和搜索在服务端完成，前端仅渲染当前页；缩略图继续懒加载，预览队列和详情抽屉按 ID 读取，不受分页影响。
- 分页改造验收：首屏只请求当前页、筛选不下载全量数据、翻页保持筛选条件、引用页不因离开当前页丢失、局域网 3-5 人并发下首屏响应保持可接受。
- 浏览器自动化环境当前无可用浏览器会话，本轮未完成截图级视觉回归；已完成静态 DOM/CSS、HTTP、PPTX 渲染和脚本验证，保留该残余风险。

### 分页已落地

- 资源库现在由服务端分页接口提供数据，前端默认 12 页/页；搜索、结构标签、场景标签和标注状态会重新请求第 1 页。
- 详情、预览和 AI 引用按页面 ID 单独补取，分页只限制资源库列表，不改变已选页面和任务数据。
- 当前 550 个原始页面中，资源库实际展示 339 个有效页面；首屏请求从全量页面降为 12 个页面元数据，缩略图继续由浏览器懒加载。
- 后续可在局域网并发量明显上升后再加服务端缩略图压缩、缓存头和 SQLite 索引；当前不引入数据库迁移。

## 2026-08-24 执行状态

客户化改写闭环已达到“可审阅过程稿”阶段：源页计划、最终输出计划、客户资料证据、缺口页和质量门禁均可追溯。下一阶段不是继续扩大自动生成范围，而是优先把质量门禁中的 blocker 转成前端可操作的补资料/改计划动作，再验证销售能否在 10 分钟内完成确认。

本轮新增的工程约束：

- 没有目标客户资料时不调用逐页模型分类，直接使用规则计划；有证据时才调用 DeepSeek，并按批次重试、失败降级。
- `changePlan` 与 `outputPlan` 永久分离，所有新增页必须带证据 ID，所有源页必须能映射到输出页或明确删除。
- 源客户长段事实被直接改名时必须有逐页证据，否则质量门禁阻断交付。
- 生成缺口页时清理模板未使用的旧长文案，防止可编辑模板保留无关事实。

验收记录：

- 14 页竞品样例生成 18 页过程稿，4 页缺口页带证据；PPTX ZIP 完整且可编辑，质量门禁正确进入 review。
- 中国移动 → 可口可乐 106 页无资料回归生成 106 页过程稿，4 类客户缺口均 pending，未新增客户事实；输出质量因源 Logo/占位页/重复标题停在 review。
